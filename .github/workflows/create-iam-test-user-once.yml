name: Create Planes IAM test user once

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/create-iam-test-user-once.yml

permissions:
  contents: read

jobs:
  invoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Invoke one-time test user creator
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H 'Content-Type: application/json' \
            -d '{}' \
            'https://xfgcbxppsbwmxwsuajou.supabase.co/functions/v1/create-iam-test-user-once'
